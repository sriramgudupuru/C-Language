#include<stdio.h>
int main()
{
	char inp[200];
	char pro[200];
	gets(inp);
	//scanf("enter the products that you want to remove %s\n",pro);
	    for(int i=0;inp[i]!='\0';i++)
    {
    	printf("%d",i);
	}
}